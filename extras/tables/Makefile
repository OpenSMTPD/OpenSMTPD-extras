.include <bsd.own.mk>

SUBDIR	 =	table-ldap
SUBDIR	+=	table-mysql
SUBDIR	+=	table-passwd
SUBDIR	+=	table-postgres
SUBDIR	+=	table-python
SUBDIR	+=	table-socketmap
SUBDIR	+=	table-sqlite
SUBDIR	+=	table-stub

.include <bsd.subdir.mk>
